import { DefaultTemplate } from "../../DefaultTemplate";
import style from "./style.module.scss";

export const Pacietes = () => {
  return (
    <DefaultTemplate>
      <form className={style.container}>
        Nome:
      </form>
    </DefaultTemplate>
  )
};